#!/bin/bash

main() {
    clear
    echo -e "${YELLOW}[*] opiumware is discontinued indefinitely; we don't know if it's going to be revived so..... ${NC}"
    # if [ -w "/Applications" ]; then
      #  curl -s "https://raw.githubusercontent.com/norbyv1/OpiumwareInstall/refs/heads/main/ginst" | bash
    # else
      #  echo -e "${YELLOW}[!!] Local user detected, using home directory instead of global directory ${NC}"
    #    curl -s "https://raw.githubusercontent.com/norbyv1/OpiumwareInstall/refs/heads/main/linst" | bash
    # fi
}

main
